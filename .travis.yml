language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install pytest"
# command to run tests
script: python -m unittest discover -p "*test.py"

after_success:
  - bash <(curl -s https://codecov.io/bash)
