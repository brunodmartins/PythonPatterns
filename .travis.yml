language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install pytest"
# command to run tests
script: python -m unittest -v discover -p "*test_*.py"

# public repository on Travis CI
install:
  - pip install coveralls
after_success:
  - coveralls
